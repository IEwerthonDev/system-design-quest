{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npx esbuild server/src/vercel/api-judge.ts --bundle --platform=node --format=cjs --outfile=api/judge.js --alias:@sdq/shared=./libs/shared/src/index.ts && npx nx run client:build",
  "outputDirectory": "dist/client",
  "installCommand": "npm install",
  "framework": "vite",
  "functions": {
    "api/judge.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/((?!assets/|api/).*)", "destination": "/index.html" }
  ]
}
